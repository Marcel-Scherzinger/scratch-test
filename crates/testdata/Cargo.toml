[package]
name = "testdata"
version = "0.1.0"
edition = "2024"

[dependencies]
interpreter = { path = "../interpreter" }
log = "0.4.28"
derive_more = { version = "2.0.1", features = ["display", "from"] }
derive-getters = "0.5.0"
