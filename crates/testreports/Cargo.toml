[package]
name = "testreports"
version = "0.1.0"
edition = "2024"

[dependencies]
interpreter = { path = "../interpreter" }
model = { path = "../model" }
derive_more = { version = "2.0.1", features = ["display", "from"] }
itertools = "0.14.0"
