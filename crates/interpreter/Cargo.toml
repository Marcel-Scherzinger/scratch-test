[package]
name = "interpreter"
version = "0.1.0"
edition = "2024"

[dependencies]
model = { path = "../model" }
thiserror = { workspace = true }
derive_more = { version = "2.0.1", features = ["display", "from"] }
log = "0.4.28"
itertools = "0.14.0"
derive-getters = "0.5.0"
